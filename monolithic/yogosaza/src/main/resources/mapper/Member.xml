<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hyewadong.yogosaza.mapper.MemberMapper">

    <!-- 회원가입 -->
    <!-- insert문은 일반적으로 삽입된 행의 수를 반환하도록 설계되어 있다.    -->
    <insert id="registerMember" parameterType="MemberEntity">
        insert into member(member_id, member_pwd, member_name, member_phone, member_address, member_create_date)
        values(#{memberId}, #{memberPwd}, #{memberName}, #{memberPhone}, #{memberAddress}, #{memberCreateDate})
    </insert>


    <!-- 회원 상세 조회  -->
    <select id="getMember" parameterType="int" resultType="hyewadong.yogosaza.dto.MemberGetDto">
        select member_id, member_name, member_phone, member_address, member_create_date
        from member
        where member_seq = #{memberSeq}
    </select>


<!--    &lt;!&ndash; 멤버 전부 조회  &ndash;&gt;-->
<!--    <select id="getMembers" resultType="MemberEntity">-->
<!--        &lt;!&ndash; select * from promises where member_seq = #{memberSeq}select &ndash;&gt;-->
<!--        select *-->
<!--        from member-->
<!--    </select>-->


<!--    &lt;!&ndash; 멤버 변경  &ndash;&gt;-->
<!--    <update id="modifyMember" parameterType="MemberEntity">-->
<!--        update member-->
<!--        <trim prefix="set" suffixOverrides=",">-->
<!--            <if test="name != null">name = #{name},</if>-->
<!--        </trim>-->
<!--        where seq = #{seq}-->
<!--    </update>-->

<!--    &lt;!&ndash; 멤버 삭제 &ndash;&gt;-->
<!--    <delete id="removeMember" parameterType="int">-->
<!--        delete from member-->
<!--        where seq = #{seq}-->
<!--    </delete>-->

</mapper>